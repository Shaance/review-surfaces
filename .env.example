# Optional live enrichment credentials.
# Keep real values in .env.local and copy them into worktrees with ./scripts/copy-env.sh.
GOOGLE_GENERATIVE_AI_API_KEY=
REVIEW_SURFACES_AI_MODEL=gemini-2.5-flash
