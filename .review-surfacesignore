# review-surfaces.COLLECTOR.6 / review-surfaces.PRIVACY.3
# Exclude local credentials and raw logs before indexing or remote provider use.
.env
.env.*
!.env.example
**/.dev.vars
**/*.pem
**/*.key
**/id_rsa
**/id_ed25519
.claude/
.review-surfaces/agent_handoff.md
.review-surfaces/feedback/raw/**
.review-surfaces/inputs/conversation.raw.*
# review-surfaces.PRIVACY.7 / D9: the normalized conversation log is a persisted
# surface; exclude it independent of --output-dir (the .gitignore .review-surfaces/*
# coverage is conditional on the default output dir).
.review-surfaces/inputs/conversation.normalized.*
